# Telegram bot framework (v20+ async API)
python-telegram-bot==21.6

# Load .env file for local development (no-op when env vars are already set)
python-dotenv==1.0.1

# httpx is required internally by python-telegram-bot
httpx==0.27.2
