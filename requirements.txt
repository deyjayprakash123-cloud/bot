# ── Telegram bot framework ────────────────────────────────────────────────────
# v21.x uses the modern Application / ConversationHandler API.
# Compatible with Python 3.12+.
python-telegram-bot==21.6

# ── HTTP client (required by python-telegram-bot internally) ──────────────────
# Must stay within the range python-telegram-bot 21.x declares.
httpx==0.27.2

# ── Environment variable loader ───────────────────────────────────────────────
# No-op when vars are already set (e.g. on Render); safe to include.
python-dotenv==1.0.1
